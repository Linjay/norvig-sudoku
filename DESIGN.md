# 数独 Web 可视化解题器 — 设计文档

> 基于本仓库的 Norvig 数独求解模块（`sudoku.en.cc`），构建一个 Web 页面：
> 提供 10 道按难度分布的数独题，用户选题后调用 C++ 解题模块获取**解题路径**，
> 并在页面上逐步渲染求解过程。
>
> 状态：设计稿（待评审）。实现将在本文档确认后开始。

---

## 1. 需求

### 1.1 目标

- 复用现有 C++ 求解模块（约束传播 + MRV + 回溯），而不是用 JS 重写算法。
- 让用户直观看到"机器是怎么解数独的"：传播、唯一候选、唯一位置、猜测、回溯。
- 零配置本地运行：一条命令启动，浏览器打开即用。

### 1.2 功能需求（FR）

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-1 | 内置 10 道题，难度分布：简单 ×3、中等 ×3、困难 ×2、专家 ×2 | P0 |
| FR-2 | 题目列表展示难度徽标、编号、缩略信息（提示数个数） | P0 |
| FR-3 | 选中题目后渲染 9×9 初始盘面（givens 与空格视觉区分） | P0 |
| FR-4 | 点击"求解"调用 C++ 解题模块，返回结构化解题路径（trace） | P0 |
| FR-5 | 逐步回放解题路径：播放/暂停、单步前进/后退、跳到结尾、调速（0.5×/1×/2×/4×） | P0 |
| FR-6 | 回放时高亮当前格、标注步骤类型（着色区分），侧栏滚动显示步骤日志 | P0 |
| FR-7 | 展示求解统计：总步数、猜测次数、回溯次数、求解耗时 | P1 |
| FR-8 | 回放粒度切换："关键步"（仅赋值/猜测/回溯）与"全部步"（含逐条候选删除） | P1 |
| FR-9 | 支持粘贴自定义 81 字符题串求解（`.`/`0` 表示空格） | P2 |
| FR-10 | 无解或非法输入时给出明确错误提示，页面不崩溃 | P0 |

### 1.3 非功能需求（NFR）

- **性能**：内置 10 题求解 API 响应 < 500ms（题库题目均预先验证可快速求解）；前端回放动画 ≥ 30fps。
- **trace 体积**：单题 trace JSON < 2MB；超限时后端自动降级为"关键步"粒度并在响应中标记 `truncated`。
- **兼容性**：现代浏览器（Chrome/Firefox/Safari 近两年版本），无需构建工具链，前端零依赖（原生 HTML/CSS/JS）。
- **可移植性**：C++ 侧修复现有的 macOS 专属计时代码（`_STRUCT_TIMEVAL`），Linux/macOS 均可编译。
- **安全**：自定义输入严格校验（长度、字符集），子进程调用不经过 shell，杜绝注入。

### 1.4 明确不做（Out of Scope）

- 用户手动做题（填数、笔记）——本项目定位是"解题过程可视化"，不是数独游戏。
- 题目生成器——题库来自仓库自带的 `data/top95.txt`、`data/hardest.txt` 及公开简单题，人工挑选后固化。
- 多用户/持久化/部署上云。

---

## 2. 题库设计（10 题及难度标定）

### 2.1 难度定义

难度不靠人工感觉，而是**用求解器自身的行为指标标定**，可复现：

| 难度 | 标定规则（求解 trace 统计） | 数量 | 来源 |
|---|---|---|---|
| 简单 Easy | 猜测次数 = 0（纯约束传播即可解出） | 3 | 公开简单题（如 Norvig 文中 easy50 一类） |
| 中等 Medium | 猜测 1 ~ 5 次 | 3 | `data/top95.txt` 中筛选 |
| 困难 Hard | 猜测 6 ~ 30 次 | 2 | `data/top95.txt` 中筛选 |
| 专家 Expert | 猜测 > 30 次，或来自 hardest 集 | 2 | `data/hardest.txt` 中筛选 |

### 2.2 题库生成流程

写一个一次性标定脚本 `tools/calibrate.sh`：对候选题逐题运行求解器（trace 模式），
按上表规则分档，从每档中挑出满足数量的题，固化为 `web/puzzles.json`：

```json
[
  {
    "id": "easy-1",
    "difficulty": "easy",
    "givens": 38,
    "puzzle": "81 字符题串",
    "stats": { "guesses": 0, "backtracks": 0, "keySteps": 43 }
  }
]
```

固化后的 `puzzles.json` 进仓库，运行时不再依赖标定脚本；标定脚本保留，便于日后换题。
每题入库前验证：有唯一可达解、求解耗时 < 200ms、trace 体积达标。

---

## 3. 交互设计

### 3.1 页面布局（单页应用，双栏）

```
+----------------------------------------------------------+
|  Norvig Sudoku Solver 可视化              [自定义题输入]  |
+------------------+---------------------------------------+
|  题目列表         |          9×9 棋盘区                   |
|  ┌────────────┐  |   ┌─────────────────────┐             |
|  │ E1 简单 38格│  |   │  格子：given/推导/猜测 │             |
|  │ E2 简单 ...│  |   │  当前步高亮 + 候选数    │             |
|  │ M1 中等 ...│  |   └─────────────────────┘             |
|  │ H1 困难 ...│  |   [▶播放] [⏸] [◀单步] [单步▶] [⏭结尾]   |
|  │ X1 专家 ...│  |   速度 [0.5x 1x 2x 4x]  粒度[关键步|全部]|
|  └────────────┘  |   进度条 ▓▓▓▓░░░░░ 128/402            |
|                  +---------------------------------------+
|  统计面板         |          步骤日志（自动滚动）           |
|  猜测:3 回溯:1    |   #127 [传播] R4C5 删除候选 7 (因 R4C1=7)|
|  耗时:0.46ms     |   #128 [唯一候选] R4C5 = 3              |
+------------------+---------------------------------------+
```

### 3.2 核心交互流程

1. **进入页面** → 加载 `puzzles.json`，左栏按难度分组渲染 10 道题；默认不选中，棋盘区显示引导文案。
2. **点击题目** → 棋盘渲染初始盘面（givens 深色粗体，空格浅色）；统计面板清空；出现"求解"按钮。
3. **点击求解** → 调用 `POST /api/solve`；等待期间按钮转为 loading（题库题通常瞬时返回）。
4. **收到 trace** → 自动定位到第 0 步（初始盘面），播放控件激活；用户可直接点播放，或单步调试式地逐步查看。
5. **回放中**：
   - 当前操作格高亮描边；本步骤类型决定颜色（见 3.3）。
   - 空格内以小号字显示剩余候选数字（3×3 微网格），随删除步骤实时减少。
   - 步骤日志同步滚动并高亮当前行；点击日志任意行可跳转到该步。
   - **单步后退**通过"从第 0 步重放到第 N-1 步"实现（状态快照见 4.4），对用户无感。
6. **回放结束** → 盘面即完整解；若无解，回放到矛盾点后弹出"此题无解"提示（题库题不会出现，仅自定义输入可能触发）。
7. **切换题目/修改自定义输入** → 若正在回放，先弹确认（"将丢弃当前回放进度"）。

### 3.3 步骤类型与视觉语言

| 步骤类型 | 含义 | 视觉 |
|---|---|---|
| `given` | 初始提示数 | 深灰粗体（静态，不参与回放动画） |
| `eliminate` | 从某格删除一个候选 | 该候选数字红色闪烁后消失（仅"全部步"粒度显示） |
| `naked_single` | 唯一候选：格内只剩一个候选，确定填入 | 蓝色数字 + 蓝色描边脉冲 |
| `hidden_single` | 唯一位置：某行/列/宫中该值只剩一处可放 | 绿色数字 + 所在 group 短暂着色 |
| `guess` | 搜索分支：MRV 选格试探 | 橙色数字 + 橙色描边，日志标注分支深度 |
| `backtrack` | 回溯：撤销某分支后的全部填入 | 被撤销格灰色淡出，盘面回滚到分支快照 |

配色遵循色弱友好原则（蓝/橙为主对比），类型同时以图标+文字呈现，不单靠颜色。

---

## 4. 技术方案

### 4.1 总体架构

```
浏览器（原生 HTML/CSS/JS 单页）
   │  GET  /            静态页面
   │  GET  /api/puzzles 题库(读 puzzles.json)
   │  POST /api/solve   {puzzle, granularity}
   ▼
轻量后端 Node.js http 服务（server.js，零 npm 依赖）
   │  child_process.execFile（不经 shell）
   ▼
C++ 求解器二进制 sudoku_trace（--trace 模式，stdout 输出 JSON）
```

**选型理由**：
- 用户核心诉求是"调用现有解题模块"→ 保留 C++ 二进制为求解内核，后端只做薄封装。
- Node 零依赖 `http` 模块即可满足两个接口，避免引入 npm/pip 依赖管理负担。
- 备选方案对比：
  - *Emscripten 编 WASM 纯静态页*：最优雅（无后端），但引入工具链依赖，本环境不保证可用，列为后续演进方向。
  - *构建期预生成 10 份 trace JSON 静态托管*：最简单，但不满足"操作时调用解题模块"与自定义输入（FR-9），仅作为降级方案保留能力。

### 4.2 C++ 模块改造（`sudoku.en.cc` → `src/`）

现有文件重构为三部分，**算法逻辑不变**：

```
src/
  sudoku.h / sudoku.cc     # Possible + Sudoku 类（现有代码抽出）
  trace.h / trace.cc       # 步骤记录器（新增）
  main_cli.cc              # 保留原命令行行为（读 stdin 逐行解题）
  main_trace.cc            # 新入口：--trace，读题串输出 JSON trace
Makefile                   # make cli / make trace / make test
```

改造点：

1. **注入 TraceRecorder**：`Sudoku` 增加可选的记录器指针（默认为空，零开销）。在
   `assign`、`eliminate` 的四个关键分支处埋点：删除候选、naked single（`N==1` 分支）、
   hidden single（`n==1` 分支）、以及 `solve()` 中的 guess/backtrack。
2. **顺带修复既有 bug**（上一轮分析确认的）：
   - `_STRUCT_TIMEVAL` 换成 `std::chrono::steady_clock`（跨平台 + 修掉微秒借位 bug）；
   - 构造函数遇矛盾盘面时置 `_valid=false` 并被 trace 模式报告为 `invalid_puzzle`；
   - 输入长度/字符集校验（恰好 81 个有效字符）；
   - `Possible::eliminate` 的 XOR 改为 `_b &= ~(1<<(i-1))`。
3. **trace 体积控制**：记录器带上限（默认 200,000 原始步）；超限后停止记录 `eliminate`
   细粒度步、只记关键步，并在输出中置 `"truncated": true`。

### 4.3 Trace JSON 协议（后端 → 前端）

```json
{
  "solved": true,
  "puzzle": "输入题串",
  "solution": "81 字符解串",
  "truncated": false,
  "stats": { "guesses": 3, "backtracks": 1, "eliminations": 412,
             "keySteps": 61, "totalSteps": 473, "timeUs": 468 },
  "steps": [
    { "t": "eliminate",     "cell": 4,  "val": 7, "by": 0 },
    { "t": "naked_single",  "cell": 4,  "val": 3 },
    { "t": "hidden_single", "cell": 30, "val": 4, "group": "col3" },
    { "t": "guess",         "cell": 40, "val": 2, "depth": 1, "cands": [2,7] },
    { "t": "backtrack",     "depth": 1 }
  ]
}
```

约定：`cell` 为 0~80 索引；`guess` 携带当时的完整候选，供前端展示"为什么选这格"；
`backtrack` 表示丢弃 depth ≥ 该值的所有步骤（前端据此回滚）。无解时
`solved:false`，`steps` 保留至最后一次矛盾，便于展示"证明无解"的过程。

### 4.4 前端状态模型（回放引擎）

- trace 一次性拉取后**纯前端回放**，播放过程不再请求后端。
- 盘面状态 = `Array(81)` 的候选位图（与 C++ 同构，直接用 JS 位运算模拟 `Possible`）。
- 回放器维护 `stepIndex`；**每逢 `guess` 步保存一份盘面快照**，`backtrack` 直接恢复快照——
  与求解器的分支语义一一对应，也让"单步后退/任意跳转"实现为"取最近快照 + 前向重放"，
  复杂度 O(两个 guess 之间的步数)，实测远小于一帧。
- 渲染层与状态层分离：状态变更产出"脏格子集合"，只重绘脏格，保证全部步粒度下的流畅度。

### 4.5 API 定义

| 方法 | 路径 | 请求 | 响应 | 错误 |
|---|---|---|---|---|
| GET | `/api/puzzles` | — | 题库数组（见 2.2） | — |
| POST | `/api/solve` | `{"puzzle": "81字符", "granularity": "full"\|"key"}` | trace JSON（见 4.3） | 400 非法输入；422 无解（仍带 trace）；500 求解器异常 |

后端校验：`puzzle` 长度 81、字符集 `[1-9.0]`；`execFile` 传参不拼接命令行字符串；子进程 5s 超时强杀（防自定义的病态无解题，如 README 中 Q4 需 77s 的场景）并返回 504。

### 4.6 目录结构与运行方式

```
norvig-sudoku/
  src/ ...            # C++（见 4.2）
  Makefile
  server.js           # Node 后端（零依赖）
  web/
    index.html  app.js  style.css  puzzles.json
  tools/calibrate.sh  # 题库标定脚本
  tests/ ...          # 见第 5 节
```

运行：`make trace && node server.js` → 打开 `http://localhost:8000`。

---

## 5. 测试方案

### 5.1 C++ 单元/回归测试（`tests/cpp/`，make test 驱动）

| 用例 | 验证点 |
|---|---|
| 已知题正确性 | 对 README 中 Q1~Q3 及题库 10 题，解串与已知答案一致 |
| top95 全量回归 | `data/top95.txt` 95 题全部可解、每题耗时 < 2s |
| hardest 全量回归 | `data/hardest.txt` 11 题全部可解 |
| 无解题 | README Q4 输入 → `solved:false` 且进程正常退出 |
| 非法输入 | 长度 ≠ 81 / 非法字符 / 初始矛盾（同宫两个相同 given）→ 结构化错误，无 UB |
| **trace 自洽性（核心）** | 独立的 `replay` 校验器：从初始盘面按 steps 逐步重放，断言 ① 每步操作在当时状态下合法（如 naked_single 时该格确实只剩该候选）② 重放终态 == `solution` ③ `stats` 计数与 steps 实际统计一致 |
| trace 截断 | 构造超限场景，验证 `truncated` 标记与关键步完整性 |
| Possible 位运算 | count/val/eliminate/is_on 全 512 种位图穷举断言（对照朴素实现） |

### 5.2 后端 API 测试（`tests/api.test.js`，Node 内置 test runner）

- `/api/puzzles` 返回 10 题、难度分布 3/3/2/2、字段完整。
- `/api/solve`：题库 10 题逐一请求，`solved:true` 且响应 < 500ms。
- 错误路径：长度错误→400；无解题→422 且带 trace；超时题（构造）→504。
- 注入防护：puzzle 中含 `; rm -rf` 等字符 → 400，且不产生任何子进程副作用。
- 并发：10 个并发 solve 请求全部正确返回。

### 5.3 前端测试

- **回放引擎单元测试**（`tests/replay.test.js`，逻辑层不依赖 DOM）：
  对题库 10 题的真实 trace 断言：跳转到任意步的状态 == 从头重放到该步的状态（快照机制正确性）；`backtrack` 后盘面与对应 `guess` 前一致；播放到末步 == solution。
- **E2E（Playwright，环境已预装 Chromium）**：
  1. 打开页面 → 断言 10 道题按难度分组渲染；
  2. 选中一道中等题 → 断言 givens 渲染数量正确；
  3. 点击求解 → 播放 → 等待结束 → 逐格断言棋盘 == 已知解；
  4. 单步后退 3 步再前进 3 步 → 断言盘面一致；
  5. 粘贴无解题 → 断言错误提示出现且页面可继续操作；
  6. 粘贴非法串 → 断言前端校验提示，未发请求。

### 5.4 验收标准（Definition of Done）

- 上述三层测试全绿；`make test && node --test && npx playwright test` 一键可跑。
- 题库 10 题在页面上从选题到回放完成的全流程人工走查通过。
- README 增补 Web 版的构建、运行、测试说明。

---

## 6. 实施里程碑

| # | 内容 | 产出 |
|---|---|---|
| M1 | C++ 重构 + bug 修复 + trace 模式 + cpp 测试 | `make trace`/`make test` 通过 |
| M2 | 题库标定，固化 `puzzles.json` | 10 题按 3/3/2/2 分布入库 |
| M3 | Node 后端 + API 测试 | 两个接口可用 |
| M4 | 前端盘面渲染 + 回放引擎 + 单元测试 | 核心交互可用 |
| M5 | E2E 测试 + 文档 | 全部验收标准达成 |
